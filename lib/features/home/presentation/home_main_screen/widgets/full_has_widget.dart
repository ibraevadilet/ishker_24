import 'package:flutter/material.dart';

class FullHasWidget extends StatelessWidget {
  const FullHasWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FullHasWidget'),
    );
  }
}
