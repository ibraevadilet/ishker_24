import 'package:flutter/material.dart';

class BankMainScreen extends StatelessWidget {
  const BankMainScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BankMainScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
