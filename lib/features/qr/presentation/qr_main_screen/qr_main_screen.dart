import 'package:flutter/material.dart';

class QrMainScreen extends StatelessWidget {
  const QrMainScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('QrMainScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
