import 'package:flutter/material.dart';

class QrHistoryScreen extends StatelessWidget {
  const QrHistoryScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('QrHistoryScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
